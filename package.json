{
  "name": "eco-token-deployment",
  "version": "1.0.0",
  "description": "Deployment script for EcoToken using Ethers v6 and Hardhat",
  "main": "deploy.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "ethers": "^6.6.4",
    "dotenv": "^16.3.1",
    "hardhat": "^2.22.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0"
  }
}
